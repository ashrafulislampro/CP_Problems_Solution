#include <bits/stdc++.h>
using namespace std;

void solve()
{
    int n, x;
    cin >> n;
    stack<pair<int,int>> st, fullS;
    vector<int> v;
    int i = 0;
    while(n--){
        i++;
        cin>>x;
        st.push({x, i});
    }

    
}
int main()
{
    ios::sync_with_stdio(0);
    cin.tie(0), cout.tie(0);

   
        solve();
    return 0;
}